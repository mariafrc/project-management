import { RegisterValidationPipe } from './register-validation.pipe';

describe('RegisterValidationPipe', () => {
  it('should be defined', () => {
    expect(new RegisterValidationPipe()).toBeDefined();
  });
});
